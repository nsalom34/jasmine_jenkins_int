
var getOpposite = require("../code/example.js");

describe('Write Your Test Group Descriptions Here', () => {
    it('Write Your Test Expectation Here', () => {
        // arrange
        let bool = false;

        // act
        const result = getOpposite(bool);

        // assert
        expect(result).toBe(true);
    })

    it('Write Your Test Expectation Here 2', () => {
        // arrange
        let bool = false;

        // act
        const result = getOpposite(bool);

        // assert
        expect(result).toBe(false);
    })
});
describe('Write Your Test Group Descriptions Here2', () => {
    it('Write Your Test Expectation Here', () => {
        // arrange
        let bool = false;

        // act
        const result = getOpposite(bool);

        // assert
        expect(result).toBe(true);
    })

    it('Write Your Test Expectation Here 2', () => {
        // arrange
        let bool = false;

        // act
        const result = getOpposite(bool);

        // assert
        expect(result).toBe(true);
    })
});